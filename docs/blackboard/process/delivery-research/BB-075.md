# BB-075 — Validate one useful HOW improvement on held-out delivery tasks

Status: RESEARCH INPUT; no implementation authority.

## Outcome

One measured failure drives a bounded strategy improvement or an evidence-backed keep-baseline decision.

## Current gap

A self-improvement mechanism needs real failure evidence and held-out checks to avoid optimizing its own judge.

## Source routing

- `packages/agentic-system/src/domain-execution-control.js`

Product direction: `docs/blackboard/process/delivery-product-roadmap.md`. Exact contract: `docs/blackboard/artifacts/objective/BB-075.json` / `docs/blackboard/artifacts/ready-implement-plan/BB-075.json`.

## Research questions

1. Select one observed pilot failure and identify whether strategy, context or tool configuration owns it.
2. Apply BB-059 promotion/currentness semantics using training and held-out tasks with fixed acceptance.
3. Measure improvement and regression, preserve independent evaluation and define rollback on degradation.

## Approaches and evidence

Compare Langfuse, mini-SWE-agent. Use the source links and dated eligibility metadata in the roadmap. For every selected approach record source/release, capability actually inspected, limitation, licensing/deployment fit, integration seam, rejected alternative and the smallest existing-Core option. Current candidates are not adoption decisions.

## Discriminating experiment

Evaluate baseline and one candidate on held-out tasks, attempt to alter acceptance, and recover an in-flight pre-promotion run.

Specify input fixture, process/provider identities, reset procedure, resource budget, expected output, negative case and pass/fail decision before running. Research may use bounded probes; live credentials or deployment access missing from the environment are explicit prerequisites, never synthetic evidence.

## Required product evidence

- One candidate is linked to a real pilot failure and evaluated with fixed product semantics and resource limits.
- Held-out evaluation supports PROMOTE or KEEP_BASELINE; acceptance changes and producer-authored success claims cannot decide promotion.
- Existing attempts remain pinned, new attempts use a promoted strategy only after authorization, and regression can trigger supported rollback.

## Handoff

Resolve all plan researchGaps in place. Replace candidate directions with selected decisions, narrow write scope, map each acceptance criterion to executable verification and pin the current source/dependency assumptions. Submit exact plan to Jev; only SATISFIED can make READY. No paid pilot or product implementation is authorized by a draft.

Worker direct dependencies: BB-074, BB-059. Research may proceed before these are delivered; it must not describe their future outputs as existing capability.
