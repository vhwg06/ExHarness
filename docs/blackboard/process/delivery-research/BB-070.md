# BB-070 — Deliver product from goal and partial artifacts across owned domains

Status: RESEARCH INPUT; no implementation authority.

## Outcome

The supported web product can be delivered from goal-only, partial and advanced seeds through domain-owned workloads.

## Current gap

The first useful slice does not yet demonstrate product entry, clarification and complete multi-domain delivery.

## Source routing

- `packages/agentic-system/src/domain-execution-control.js`

Product direction: `docs/blackboard/process/delivery-product-roadmap.md`. Exact contract: `docs/blackboard/artifacts/objective/BB-070.json` / `docs/blackboard/artifacts/ready-implement-plan/BB-070.json`.

## Research questions

1. Define ProjectSeed input validation and bounded product-owner clarification for missing requirements/design.
2. Bind PM/BA/SA/FE/BE/DevOps/QA responsibilities to the existing lifecycle and delivered activation contracts.
3. Compare explicit domain strategies with generic graph orchestration; preserve independent domain ownership and avoid a central next-role selector.

## Approaches and evidence

Compare LangGraph, OpenHands SDK, Playwright. Use the source links and dated eligibility metadata in the roadmap. For every selected approach record source/release, capability actually inspected, limitation, licensing/deployment fit, integration seam, rejected alternative and the smallest existing-Core option. Current candidates are not adoption decisions.

## Discriminating experiment

Run all three seed classes through the supported web product; inspect autonomous domain activation, clarification and exact closure.

Specify input fixture, process/provider identities, reset procedure, resource budget, expected output, negative case and pass/fail decision before running. Research may use bounded probes; live credentials or deployment access missing from the environment are explicit prerequisites, never synthetic evidence.

## Required product evidence

- Goal-only, requirements/design and existing-code seeds converge to the same declared acceptance semantics with actual product evidence.
- FE and BE can progress independently where obligations allow; domain publication creates authorized obligations without selecting another domain's runtime.
- Missing authoritative product decisions pause for the owner; current deployment/QA/completeness evidence is required for product outcome closure.

## Handoff

Resolve all plan researchGaps in place. Replace candidate directions with selected decisions, narrow write scope, map each acceptance criterion to executable verification and pin the current source/dependency assumptions. Submit exact plan to Jev; only SATISFIED can make READY. No paid pilot or product implementation is authorized by a draft.

Worker direct dependencies: BB-069, BB-055. Research may proceed before these are delivered; it must not describe their future outputs as existing capability.
