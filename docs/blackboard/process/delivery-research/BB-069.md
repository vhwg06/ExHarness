# BB-069 — Prove first useful agent delivery through Oracle and independent QA

Status: RESEARCH INPUT; no implementation authority.

## Outcome

One bounded real feature reaches a reviewable candidate and isolated running preview with independent acceptance.

## Current gap

No current phase receipt proves Oracle context plus real coding-agent execution plus observed usable output in one loop.

## Source routing

- `packages/agentic-system/src/oracle.js`
- `packages/agentic-system/src/domain-execution-control.js`

Product direction: `docs/blackboard/process/delivery-product-roadmap.md`. Exact contract: `docs/blackboard/artifacts/objective/BB-069.json` / `docs/blackboard/artifacts/ready-implement-plan/BB-069.json`.

## Research questions

1. Define the smallest real Backend/QA delivery path using delivered Oracle and SCM adapters without claiming the full organization lifecycle.
2. Choose deterministic API/browser checks and independent semantic acceptance; define release identity evidence.
3. Design three reset live runs, one interrupted run and one seeded defect; distinguish feasibility from reliability.

## Approaches and evidence

Compare Playwright, OpenHands SDK. Use the source links and dated eligibility metadata in the roadmap. For every selected approach record source/release, capability actually inspected, limitation, licensing/deployment fit, integration seam, rejected alternative and the smallest existing-Core option. Current candidates are not adoption decisions.

## Discriminating experiment

Deliver one request-tracker change from a reset repo to preview; independently exercise browser/API/persistence and kill/resume.

Specify input fixture, process/provider identities, reset procedure, resource budget, expected output, negative case and pass/fail decision before running. Research may use bounded probes; live credentials or deployment access missing from the environment are explicit prerequisites, never synthetic evidence.

## Required product evidence

- The real feature uses Oracle-resolved exact input refs, produces a real candidate and runs in an isolated preview whose release identity is recorded.
- Independent QA observes functional behavior and rejects a seeded defect even when the producer says success.
- At least three reset live runs and one kill/resume path have complete evidence, budgets and failure records; feasibility is reported separately from pilot value.

## Handoff

Resolve all plan researchGaps in place. Replace candidate directions with selected decisions, narrow write scope, map each acceptance criterion to executable verification and pin the current source/dependency assumptions. Submit exact plan to Jev; only SATISFIED can make READY. No paid pilot or product implementation is authorized by a draft.

Worker direct dependencies: BB-068, BB-064. Research may proceed before these are delivered; it must not describe their future outputs as existing capability.
