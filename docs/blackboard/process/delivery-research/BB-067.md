# BB-067 — Enforce bounded workspace and tool capabilities

Status: RESEARCH INPUT; no implementation authority.

## Outcome

Agent execution is constrained to its authorized workspace, tools, network and budget with enforceable cancellation.

## Current gap

Real code execution introduces filesystem, credential and side-effect boundaries not proven by abstract authority tests.

## Source routing

- `packages/agentic-system/src/domain-execution-control.js`

Product direction: `docs/blackboard/process/delivery-product-roadmap.md`. Exact contract: `docs/blackboard/artifacts/objective/BB-067.json` / `docs/blackboard/artifacts/ready-implement-plan/BB-067.json`.

## Research questions

1. Compare a container workspace, isolated local process and managed OpenHands workspace against the chosen threat model.
2. Define credential injection, egress limits and tool capability checks outside model instructions.
3. Separate Oracle read adapters from mutating execution capabilities, including MCP token/session identity.

## Approaches and evidence

Compare OpenHands SDK, MCP security practices. Use the source links and dated eligibility metadata in the roadmap. For every selected approach record source/release, capability actually inspected, limitation, licensing/deployment fit, integration seam, rejected alternative and the smallest existing-Core option. Current candidates are not adoption decisions.

## Discriminating experiment

Execute path-escape, cross-workspace, injected tool instruction, leaked-token and timeout fixtures; inspect real enforcement and cleanup.

Specify input fixture, process/provider identities, reset procedure, resource budget, expected output, negative case and pass/fail decision before running. Research may use bounded probes; live credentials or deployment access missing from the environment are explicit prerequisites, never synthetic evidence.

## Required product evidence

- Forbidden file, cross-workspace and credential access fail under the selected sandbox threat model, including indirect tool calls.
- Time/token/cost ceilings stop further dispatch and preserve a recoverable outcome; secrets are absent from persisted traces.
- Untrusted repository/MCP content cannot grant tools, change authorized domains or bypass approval for configured protected operations.

## Handoff

Resolve all plan researchGaps in place. Replace candidate directions with selected decisions, narrow write scope, map each acceptance criterion to executable verification and pin the current source/dependency assumptions. Submit exact plan to Jev; only SATISFIED can make READY. No paid pilot or product implementation is authorized by a draft.

Worker direct dependencies: BB-065. Research may proceed before these are delivered; it must not describe their future outputs as existing capability.
