# BB-074 — Measure real delivery value against direct-agent baseline

Status: RESEARCH INPUT; no implementation authority.

## Outcome

Matched delivery evidence shows whether the supported ExHarness product improves human effort and quality-adjusted cost.

## Current gap

Feasibility and architecture proofs do not establish that ExHarness is worth integrating into delivery.

## Source routing

- `packages/agentic-system/src/domain-execution-control.js`

Product direction: `docs/blackboard/process/delivery-product-roadmap.md`. Exact contract: `docs/blackboard/artifacts/objective/BB-074.json` / `docs/blackboard/artifacts/ready-implement-plan/BB-074.json`.

## Research questions

1. Ratify the BB-065 predeclared pilot protocol without post-result threshold changes; recruit two representative repository/use-case contexts.
2. Control model/tool budget, task familiarity and selection effects; report uncertainty and all failed attempts.
3. Separate accepted task quality, human review/repair time, total cost and elapsed lead time; define go/no-go.

## Approaches and evidence

Compare mini-SWE-agent, OpenHands SDK, Langfuse, METR productivity research. Use the source links and dated eligibility metadata in the roadmap. For every selected approach record source/release, capability actually inspected, limitation, licensing/deployment fit, integration seam, rejected alternative and the smallest existing-Core option. Current candidates are not adoption decisions.

## Discriminating experiment

Run matched pilot cohorts, reconcile raw time/usage/acceptance records and independently reproduce metric calculations.

Specify input fixture, process/provider identities, reset procedure, resource budget, expected output, negative case and pass/fail decision before running. Research may use bounded probes; live credentials or deployment access missing from the environment are explicit prerequisites, never synthetic evidence.

## Required product evidence

- At least the predeclared pilot sample is executed with equivalent direct-agent comparison and a held-out set; all attempts are recorded.
- Quality and value gates fixed before the run are evaluated from raw evidence including failures/retries and human intervention.
- A failed or inconclusive value gate yields an explicit no-go or follow-up scope; delivery cannot be claimed from selected successful demos.

## Handoff

Resolve all plan researchGaps in place. Replace candidate directions with selected decisions, narrow write scope, map each acceptance criterion to executable verification and pin the current source/dependency assumptions. Submit exact plan to Jev; only SATISFIED can make READY. No paid pilot or product implementation is authorized by a draft.

Worker direct dependencies: BB-071, BB-073. Research may proceed before these are delivered; it must not describe their future outputs as existing capability.
