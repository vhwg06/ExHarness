# BB-065 — Define delivery profile and executable value baseline

Status: RESEARCH INPUT; no implementation authority.

## Outcome

A fixed delivery fixture and measurement harness make direct-agent and ExHarness outcomes comparable before integration choices are made.

## Current gap

The roadmap has authority proofs but no fixed real-delivery workload or cost/review-effort baseline.

## Source routing

- `packages/agentic-system/src/domain-execution-control.js`

Product direction: `docs/blackboard/process/delivery-product-roadmap.md`. Exact contract: `docs/blackboard/artifacts/objective/BB-065.json` / `docs/blackboard/artifacts/ready-implement-plan/BB-065.json`.

## Research questions

1. Which bounded existing-repository change gives a useful first result before full lifecycle integration?
2. Compare mini-SWE-agent direct execution with an OpenHands direct baseline; isolate model/tool differences from ExHarness overhead.
3. Choose fixture stack, acceptance scenarios, human-effort accounting, resource ceilings and predeclared pilot thresholds.

## Approaches and evidence

Compare mini-SWE-agent, OpenHands SDK. Use the source links and dated eligibility metadata in the roadmap. For every selected approach record source/release, capability actually inspected, limitation, licensing/deployment fit, integration seam, rejected alternative and the smallest existing-Core option. Current candidates are not adoption decisions.

## Discriminating experiment

Run the selected direct agent on three reset fixture tasks; verify independent acceptance rejects a seeded defect and accounting includes retries.

Specify input fixture, process/provider identities, reset procedure, resource budget, expected output, negative case and pass/fail decision before running. Research may use bounded probes; live credentials or deployment access missing from the environment are explicit prerequisites, never synthetic evidence.

## Required product evidence

- A versioned fixture specifies create/list/update/validation/persistence/browser acceptance and a smaller first change; external credentials are referenced, never embedded.
- A reproducible baseline runner records attempted/accepted/failed tasks, human active time, lead time, total cost and model/tool identities; no precomputed success is substituted.
- Pilot selection, held-out partition, budgets, quality and value thresholds are fixed before comparative results; unresolved stakeholder choices are explicit.

## Handoff

Resolve all plan researchGaps in place. Replace candidate directions with selected decisions, narrow write scope, map each acceptance criterion to executable verification and pin the current source/dependency assumptions. Submit exact plan to Jev; only SATISFIED can make READY. No paid pilot or product implementation is authorized by a draft.

Worker direct dependencies: none. Research may proceed before these are delivered; it must not describe their future outputs as existing capability.
