# BB-071 — Prove requirement change and failure recovery on a live product

Status: RESEARCH INPUT; no implementation authority.

## Outcome

A changed requirement or failed domain run is repaired locally while unrelated accepted work stays current.

## Current gap

Static successful delivery does not demonstrate a usable iterative development process.

## Source routing

- `packages/agentic-system/src/domain-execution-control.js`

Product direction: `docs/blackboard/process/delivery-product-roadmap.md`. Exact contract: `docs/blackboard/artifacts/objective/BB-071.json` / `docs/blackboard/artifacts/ready-implement-plan/BB-071.json`.

## Research questions

1. Choose a requirement change crossing FE/BE/QA and another affecting only one domain.
2. Map real-provider process failures and stale outputs to the delivered Integration-H fence semantics.
3. Define exact downstream invalidation and evidence needed to resume without re-running the whole organization.

## Approaches and evidence

Compare Playwright, Restate, Temporal. Use the source links and dated eligibility metadata in the roadmap. For every selected approach record source/release, capability actually inspected, limitation, licensing/deployment fit, integration seam, rejected alternative and the smallest existing-Core option. Current candidates are not adoption decisions.

## Discriminating experiment

Change one field validation contract during execution, resume an old worker and rotate the preview release during QA.

Specify input fixture, process/provider identities, reset procedure, resource budget, expected output, negative case and pass/fail decision before running. Research may use bounded probes; live credentials or deployment access missing from the environment are explicit prerequisites, never synthetic evidence.

## Required product evidence

- A real requirement change invalidates only affected outputs and drives the owning domains to a newly verified release.
- Kill/resume and duplicate activation preserve attempt identity and one logical external publication.
- An old worker and stale deployment observation cannot close changed work; unrelated accepted outputs remain current.

## Handoff

Resolve all plan researchGaps in place. Replace candidate directions with selected decisions, narrow write scope, map each acceptance criterion to executable verification and pin the current source/dependency assumptions. Submit exact plan to Jev; only SATISFIED can make READY. No paid pilot or product implementation is authorized by a draft.

Worker direct dependencies: BB-070, BB-057. Research may proceed before these are delivered; it must not describe their future outputs as existing capability.
