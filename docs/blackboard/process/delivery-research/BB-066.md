# BB-066 — Integrate a real coding-agent execution strategy

Status: RESEARCH INPUT; no implementation authority.

## Outcome

One released claim dispatches a real agent through a pinned execution strategy and resumes the same attempt after interruption.

## Current gap

The existing runtime adapter contract alone does not prove a real coding-agent session, workspace and result can survive interruption.

## Source routing

- `packages/agentic-system/src/domain-execution-control.js`

Product direction: `docs/blackboard/process/delivery-product-roadmap.md`. Exact contract: `docs/blackboard/artifacts/objective/BB-066.json` / `docs/blackboard/artifacts/ready-implement-plan/BB-066.json`.

## Research questions

1. Compare OpenHands SDK adapter, minimal CLI adapter and reuse of the existing Core loop; select one primary implementation.
2. Compare existing Core recovery with Restate/Temporal and LangGraph only for domain-local runtime needs; document complexity and licensing tradeoffs.
3. Define binding of runtime session, workspace revision, model/config, cancellation and unknown outcome to the existing attempt.

## Approaches and evidence

Compare OpenHands SDK, mini-SWE-agent, Restate, Temporal, LangGraph. Use the source links and dated eligibility metadata in the roadmap. For every selected approach record source/release, capability actually inspected, limitation, licensing/deployment fit, integration seam, rejected alternative and the smallest existing-Core option. Current candidates are not adoption decisions.

## Discriminating experiment

Kill the adapter after provider/session creation and after patch creation; recover using persisted identities and detect duplicate dispatch.

Specify input fixture, process/provider identities, reset procedure, resource budget, expected output, negative case and pass/fail decision before running. Research may use bounded probes; live credentials or deployment access missing from the environment are explicit prerequisites, never synthetic evidence.

## Required product evidence

- A real agent edits the declared fixture through runtimeAdapter.dispatch and reports exact execution and source evidence.
- Fresh-process recover reuses the same attempt/session or reports an explicit unresolved outcome; it cannot silently create a second semantic attempt.
- Pinned strategy/config and authorized scope survive policy change, timeout and cancellation; runtime success never self-accepts delivery.

## Handoff

Resolve all plan researchGaps in place. Replace candidate directions with selected decisions, narrow write scope, map each acceptance criterion to executable verification and pin the current source/dependency assumptions. Submit exact plan to Jev; only SATISFIED can make READY. No paid pilot or product implementation is authorized by a draft.

Worker direct dependencies: BB-065, BB-048. Research may proceed before these are delivered; it must not describe their future outputs as existing capability.
