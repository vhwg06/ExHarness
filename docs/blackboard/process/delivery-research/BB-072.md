# BB-072 — Make delivery runs observable recoverable and cost bounded

Status: RESEARCH INPUT; no implementation authority.

## Outcome

An operator can diagnose, stop and recover runs with attributable costs and durable evidence.

## Current gap

Internal provenance needs real runtime/provider mapping, retention and operational recovery procedures.

## Source routing

- `packages/agentic-system/src/domain-execution-control.js`

Product direction: `docs/blackboard/process/delivery-product-roadmap.md`. Exact contract: `docs/blackboard/artifacts/objective/BB-072.json` / `docs/blackboard/artifacts/ready-implement-plan/BB-072.json`.

## Research questions

1. Compare Langfuse/OTel export with a minimal operator CLI; keep canonical evidence independent from a tracing vendor.
2. Specify storage durability, backup/restore, retention/redaction and unknown external-effect reconciliation for single tenancy.
3. Define service health, blocked-owner explanations, budget alarms and cancellation semantics.

## Approaches and evidence

Compare Langfuse, Temporal, Restate. Use the source links and dated eligibility metadata in the roadmap. For every selected approach record source/release, capability actually inspected, limitation, licensing/deployment fit, integration seam, rejected alternative and the smallest existing-Core option. Current candidates are not adoption decisions.

## Discriminating experiment

Disconnect telemetry, exhaust a budget, restart services and restore a backup; reconcile provider usage and pending external effects.

Specify input fixture, process/provider identities, reset procedure, resource budget, expected output, negative case and pass/fail decision before running. Research may use bounded probes; live credentials or deployment access missing from the environment are explicit prerequisites, never synthetic evidence.

## Required product evidence

- A fresh operator can explain blockers and inspect exact task/attempt/model/runtime/SCM identities without model-inferred provenance.
- Cost and duration include failed/retried provider calls; missing usage is explicit and cannot appear as zero.
- Backup/restore and process restart recover canonical runs; lost telemetry cannot grant or revoke product acceptance.

## Handoff

Resolve all plan researchGaps in place. Replace candidate directions with selected decisions, narrow write scope, map each acceptance criterion to executable verification and pin the current source/dependency assumptions. Submit exact plan to Jev; only SATISFIED can make READY. No paid pilot or product implementation is authorized by a draft.

Worker direct dependencies: BB-068, BB-058. Research may proceed before these are delivered; it must not describe their future outputs as existing capability.
